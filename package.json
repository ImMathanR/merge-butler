{
  "name": "merge_butler",
  "version": "1.0.0",
  "description": "Reply with a custom message when user reports a PR is merged",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "immathanr",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.5.0",
    "axios": "^0.21.1",
    "dotenv": "^10.0.0"
  }
}
